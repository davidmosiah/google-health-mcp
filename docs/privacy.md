# Privacy

- Tokens are local and never returned by MCP tools.
- `summary` mode minimizes identifiers and source details.
- `structured` mode returns normalized Google Health data without token or secret fields.
- `raw` mode returns upstream Google Health JSON and should be used only when explicitly requested.
- This connector is not a medical device and does not provide medical advice.
