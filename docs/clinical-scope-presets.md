# Clinical scope presets

Use the smallest scope set that answers the agent question. Clinical/ECG/IRN scopes are opt-in and documented in capabilities — never request by default.

Smoke: connection_status then capabilities → list available domains before clinical tools.
